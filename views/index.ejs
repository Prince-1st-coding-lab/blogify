<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Social Blog Platform</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Georgia&display=swap" rel="stylesheet">

  <!-- Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

  <style>
    :root {
      --bg: #f9fafb;
      --card: #ffffff;
      --text: #111827;
      --muted: #6b7280;
      --border: #e5e7eb;
      --accent: #2563eb;
      --shadow: 0 4px 14px rgba(0,0,0,0.06);
    }

    body.dark {
      --bg: #0f172a;
      --card: #020617;
      --text: #e5e7eb;
      --muted: #94a3b8;
      --border: #1e293b;
      --accent: #3b82f6;
      --shadow: 0 6px 18px rgba(0,0,0,0.4);
    }

    body {
      margin: 0;
      font-family: Inter, sans-serif;
      background: var(--bg);
      color: var(--text);
    }

    a { text-decoration: none; color: inherit; }

    /* Navbar */
    header {
      position: sticky;
      top: 0;
      background: var(--card);
      border-bottom: 1px solid var(--border);
      z-index: 100;
    }

    .nav {
      max-width: 1200px;
      margin: auto;
      padding: 12px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .nav .logo { font-weight: 600; font-size: 1.1rem; }

    .nav-links a {
      margin: 0 10px;
      font-size: 0.95rem;
      color: var(--muted);
    }

    .nav-links a:hover { color: var(--text); }

    .search {
      flex: 1;
      max-width: 320px;
      margin: 0 20px;
    }

    .search input {
      width: 100%;
      padding: 8px 12px;
      border-radius: 20px;
      border: 1px solid var(--border);
    }

    .auth-btn {
      padding: 8px 14px;
      border-radius: 20px;
      background: var(--accent);
      color: #fff;
      font-size: 0.9rem;
    }

    /* Hero */
    .hero {
      max-width: 1200px;
      margin: 40px auto;
      padding: 0 20px;
      display: grid;
      grid-template-columns: 1.2fr 1fr;
      gap: 30px;
    }

    .hero h1 {
      font-size: 2.2rem;
      margin-bottom: 10px;
    }

    .hero p {
      color: var(--muted);
      font-size: 1.05rem;
    }

    .featured {
      background: var(--card);
      border-radius: 14px;
      overflow: hidden;
      box-shadow: var(--shadow);
    }

    .featured img {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }

    .featured-content {
      padding: 16px;
    }

    /* Layout */
    .container {
      max-width: 1200px;
      margin: auto;
      padding: 0 20px;
      display: grid;
      grid-template-columns: 2.5fr 1fr;
      gap: 30px;
    }

    /* Filters */
    .filters {
      display: flex;
      gap: 10px;
      overflow-x: auto;
      margin-bottom: 20px;
    }

    .filters button {
      border: 1px solid var(--border);
      background: var(--card);
      padding: 6px 14px;
      border-radius: 20px;
      cursor: pointer;
      font-size: 0.85rem;
    }

    /* Post Card */
    .post {
      background: var(--card);
      border-radius: 14px;
      padding: 16px;
      box-shadow: var(--shadow);
      margin-bottom: 20px;
      transition: transform 0.2s ease;
    }

    .post:hover { transform: translateY(-3px); }

    .post-header {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 0.85rem;
      color: var(--muted);
    }

    .avatar {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background: #ddd;
    }

    .post h3 {
      margin: 10px 0 6px;
      font-size: 1.1rem;
    }

    .post p {
      font-family: Georgia, serif;
      color: var(--muted);
    }

    .actions {
      display: flex;
      gap: 18px;
      margin-top: 12px;
      font-size: 0.85rem;
      color: var(--muted);
    }

    .actions span { cursor: pointer; }

    /* Sidebar */
    aside {
      position: sticky;
      top: 90px;
    }

    .side-card {
      background: var(--card);
      padding: 16px;
      border-radius: 14px;
      box-shadow: var(--shadow);
      margin-bottom: 20px;
    }

    /* Footer */
    footer {
      margin-top: 60px;
      padding: 30px 20px;
      background: var(--card);
      border-top: 1px solid var(--border);
      text-align: center;
      font-size: 0.85rem;
      color: var(--muted);
    }

    /* Responsive */
    @media (max-width: 900px) {
      .hero { grid-template-columns: 1fr; }
      .container { grid-template-columns: 1fr; }
      aside { display: none; }
      .search { display: none; }
    }
  </style>
</head>
<body>

<header>
  <div class="nav">
    <div class="logo">Blogify</div>
    <div class="nav-links">
      <a href="#">Home</a>
      <a href="#">Categories</a>
      <a href="#">Trending</a>
      <a href="#">About</a>
    </div>
    <div class="search">
      <input type="text" placeholder="Search posts or authors...">
    </div>
    <div style="display:flex;gap:10px;align-items:center;">
      <button id="themeToggle" style="background:none;border:1px solid var(--border);border-radius:50%;width:36px;height:36px;cursor:pointer;">
        <i class="fa-solid fa-moon"></i>
      </button>
      <a class="auth-btn" href="#">Login</a>
    </div>
  </div>
</header>

<section class="hero">
  <div>
    <h1>Discover stories, ideas, and knowledge from creators</h1>
    <p>Read thoughtful articles from people shaping technology, culture, and business.</p>
  </div>
  <div class="featured">
    <img src="https://picsum.photos/600/400" alt="Featured">
    <div class="featured-content">
      <h3>How the Web Is Changing in 2026</h3>
      <small>By Alex Johnson</small>
    </div>
  </div>
</section>

<main class="container">
  <section>
    <div class="filters">
      <button>Latest</button>
      <button>Most Liked</button>
      <button>Most Commented</button>
      <button>Trending</button>
    </div>

    <article class="post">
      <div class="post-header">
        <div class="avatar"></div>
        <span>Jane Doe · Jan 15</span>
      </div>
      <h3>Why Developers Love Clean UI</h3>
      <p>Clean interfaces improve focus, readability, and overall user satisfaction...</p>
      <div class="actions">
        <span><i class="fa-regular fa-thumbs-up"></i> 24</span>
        <span><i class="fa-regular fa-comment"></i> 5</span>
        <span><i class="fa-regular fa-bookmark"></i></span>
        <span><i class="fa-solid fa-share"></i></span>
      </div>
    </article>

    <article class="post">
      <div class="post-header">
        <div class="avatar"></div>
        <span>Prince · Jan 12</span>
      </div>
      <h3>Express.js Tips for Beginners</h3>
      <p>Learn practical Express.js patterns to build scalable backends faster...</p>
      <div class="actions">
        <span><i class="fa-regular fa-thumbs-up"></i> 41</span>
        <span><i class="fa-regular fa-comment"></i> 9</span>
        <span><i class="fa-regular fa-bookmark"></i></span>
        <span><i class="fa-solid fa-share"></i></span>
      </div>
    </article>
  </section>

  <aside>
    <div class="side-card">
      <h4>Trending Posts</h4>
      <p>• The Future of JavaScript</p>
      <p>• AI in Web Development</p>
    </div>
    <div class="side-card">
      <h4>Top Authors</h4>
      <p>Alex Johnson</p>
      <p>Jane Doe</p>
    </div>
  </aside>
</main>

<footer>
  © 2026 Blogify · Privacy · Terms · Contact
</footer>

<script>
  const toggle = document.getElementById('themeToggle');
  const icon = toggle.querySelector('i');

  if (localStorage.theme === 'dark') {
    document.body.classList.add('dark');
    icon.classList.replace('fa-moon', 'fa-sun');
  }

  toggle.onclick = () => {
    document.body.classList.toggle('dark');
    const isDark = document.body.classList.contains('dark');
    localStorage.theme = isDark ? 'dark' : 'light';
    icon.classList.toggle('fa-moon', !isDark);
    icon.classList.toggle('fa-sun', isDark);
  };
</script>
</body>
</html>
