<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Blog Post</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --bg:#f9fafb;
      --card:#ffffff;
      --text:#111827;
      --muted:#6b7280;
      --primary:#4f46e5;
      --border:#e5e7eb;
    }

    body{
      margin:0;
      font-family:'Inter',sans-serif;
      background:var(--bg);
      color:var(--text);
    }

    header{
      background:var(--card);
      border-bottom:1px solid var(--border);
      padding:18px 28px;
      display:flex;
      justify-content:space-between;
      align-items:center;
      position:sticky;
      top:0;
      z-index:10;
    }

    header h1{margin:0;font-size:20px;}
    header a{text-decoration:none;color:var(--muted);margin-left:18px;font-weight:500;}
    header a:hover{color:var(--primary);}

    .container{
      max-width:900px;
      margin:50px auto;
      padding:0 24px;
    }

    .post-header h2{
      font-size:36px;
      margin-bottom:12px;
      line-height:1.2;
    }

    .post-meta{
      color:var(--muted);
      font-size:14px;
      margin-bottom:28px;
    }

    .post-image{
      width:100%;
      height:360px;
      background:#e5e7eb;
      border-radius:16px;
      margin-bottom:30px;
      object-fit:cover;
    }

    .post-content{
      font-size:17px;
      line-height:1.8;
      
}


    .post-content h3{margin-top:36px;}
    .post-content p{margin-bottom:18px;}

    .post-footer{
      margin-top:50px;
      padding-top:30px;
      border-top:1px solid var(--border);
      display:flex;
      justify-content:space-between;
      align-items:center;
      flex-wrap:wrap;
      gap:12px;
    }

    .tag{
      background:#eef2ff;
      color:#4338ca;
      padding:6px 14px;
      border-radius:999px;
      font-size:13px;
    }

    .actions button{
      background:none;
      border:1px solid var(--border);
      padding:8px 14px;
      border-radius:10px;
      cursor:pointer;
    }

    .actions button:hover{
      border-color:var(--primary);
      color:var(--primary);
    }

    footer{
      margin-top:80px;
      padding:30px;
      text-align:center;
      color:var(--muted);
      border-top:1px solid var(--border);
    }

    @media(max-width:600px){
      .post-header h2{font-size:28px;}
      .post-image{height:220px;}
    }
  </style>
</head>
<body>

<header>
  <h1>My Blog</h1>
  <nav>
    <a href="/">Home</a>
    <a href="#">Categories</a>
    <a href="#">About</a>
  </nav>
</header>

<main class="container">

  <article>
    <div class="post-header">
      <h2><%= title %></h2>
      <div class="post-meta">By <%= authorName %> · <%= time %> · 5 min read</div>
    </div>

    <img class="post-image" alt="" src="<%= imageURL %>" />

    <div class="post-content">
      <%= content %>
    </div>

    <div class="post-footer">
      <div>
        <span class="tag"><%= category %></span>
      </div>
      <div class="actions">
        <button>👍 Like</button>
        <button>💬 Comment</button>
        <button>🔗 Share</button>
      </div>
    </div>
  </article>

</main>

<footer>
  © 2026 My Blog · All rights reserved
</footer>

</body>
</html>
